version: '3.8'

services:
  cryptoguard:
    build: .
    ports:
      - "8000:8000"
      - "5173:5173"
    env_file:
      - .env
    volumes:
      - cryptoguard_data:/app/backend/data
    restart: unless-stopped

volumes:
  cryptoguard_data:
